default:
    formatters:
        pretty: true

    autoload:
        "": "%paths.base%/features/bootstrap"

    suites:
        dbal:
            paths: [ "%paths.base%/vendor/kphoen/rulerz/tests/features/common" ]
            contexts: [ Context ]
